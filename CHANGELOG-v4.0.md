# 版本更新记录

本文件记录 LoudnessPlayer 每个公开版本的功能变化。版本号遵循 `主版本.功能版本.修订版本`。

## v1.3.0

发布日期：2026-07-31

### 新增

- APE 改为应用内实时解码播放，通过命名管道把 PCM/WAV 音频流交给 Media3，不生成持久化音频副本。
- APE 加入自动响度校验队列；FFmpeg 使用 EBU R128 滤镜计算整首响度和真峰值，并把解码输出直接丢弃。
- 新建个人音乐文件夹后可立即勾选已经导入的歌曲并批量加入。
- 已有个人音乐文件夹新增“管理已有歌曲”，可批量添加或移除音乐库中的歌曲。
- 新增跨格式重复歌曲过滤；标题、歌手相同且时长相差不超过 2 秒时只导入一个版本。
- 新增可手势打开的设置侧边栏；从屏幕左边缘向右滑动，或点击左上角三横线图标即可打开。

### 调整

- 移除 APE 自动转换成 FLAC 并保存到 `Music/LoudnessPlayer` 的流程。
- 响度设置、文件导入、整个文件夹导入、全机扫描、桌面歌词和主题选择统一移入设置侧边栏。
- 新导入批次的跨格式重复歌曲按 FLAC、APE、WAV、MP3 顺序保留；音乐库中已有版本始终优先保留。
- 去重只修改播放器音乐库，不删除手机中的任何原始文件。
- v1.2.0 已经生成的 FLAC 不会被自动删除，避免误删用户可能已单独管理的文件。

### 优化

- APE 播放只占用短生命周期的实时解码管道，不再为同一首歌占用额外的永久存储空间。
- APE 拖动进度时按目标位置重新启动解码，保留现有播放进度控制。
- APE 响度分析支持协程取消，离开播放器时会取消对应 FFmpeg 会话。
- 跨格式去重在元数据不足时不自动合并，减少误判。
- 自建文件夹继续复用已有歌曲 ID，不需要重新读取或复制音频文件。
- 首页移除响度和导入大卡片，把主要空间留给搜索、分类和歌曲列表。
- Android CI 新增 ARM32/ARM64 的 PCM、WAV、`null` 输出和 `ebur128` 能力校验。
- 保留 R8、资源裁剪、双 ARM ABI 和 WebP 封面等体积优化。

### 兼容性与限制

- 最低系统仍为 Android 7.0（API 24）。
- APK 支持 `arm64-v8a` 与 `armeabi-v7a`，不包含 x86 模拟器原生库。
- APE 开始播放、跳转和整首响度校验的速度取决于手机处理器和原文件大小。
- 跨格式去重依赖歌曲标题、歌手和时长；缺少可靠元数据时会保留文件，避免误删。
- 音乐库去重不会删除手机文件；用户仍可在其他文件管理器中看到多个格式版本。
- 响度统一适合个人本地播放，不替代广播级响度计或母带处理。

## v1.2.0

发布日期：2026-07-30

### 新增

- 新增用户自定义目标响度，可在 `-24 LUFS` 至 `-8 LUFS` 之间选择，默认仍为 `-14 LUFS`。
- 设置目标响度后自动校验歌单内尚未分析的 MP3、FLAC、WAV；已经分析的歌曲立即重新计算播放增益。
- 新增峰值保护，正增益会结合已测得的采样峰值限制，尽量避免数字削波。
- 新增 APE 自动转换为 FLAC；转换结果保存到 `Music/LoudnessPlayer`，并自动替换播放器音乐库中的 APE 条目。
- 新增 APE 转换记录，避免再次扫描手机时重复转换同一个原始文件。
- 新增桌面歌词悬浮窗、拖动和关闭功能。
- 新增普通歌词与 LRC 时间轴歌词编辑；歌词跟随播放进度更新。
- 新增顺序播放、单曲循环和随机播放。
- 新增歌曲标题自动排序，以及按歌曲名或歌手名搜索。
- 新增“按歌曲名智能分类”；歌曲名和歌手名至少共享两个有效字段时归入同一个相似组。
- 新增个人音乐文件夹；歌曲可加入多个文件夹，删除文件夹不会删除歌曲。
- 新增浅色、深色、绿色、蓝色四套可持久化主题。

### 优化

- Media3 播放队列持续保留，减少切歌时重新创建播放器造成的中断。
- 切歌和目标响度变化时加入短时音量渐变，减少突兀的音量跳变。
- 导入完成后统一按照歌曲标题排序，相同标题再按照歌手排序。
- APE 转换成功后同步迁移自建文件夹关联，并自动开始 FLAC 响度校验。
- 音乐库数据新增字段均提供默认值，兼容 v1.0.0 和 v1.1.0 本地数据。
- Debug 与 Release 构建启用 R8 和资源裁剪。
- ARM64 使用 FFmpegKit Audio；ARM32 从相同维护源码构建只含内置编解码器的最小变体。
- ARM32 原生库固定源码提交、API 24 和 NDK r27c，并记录每个二进制文件的 SHA-256。
- Android CI 会解包最终 APK，强制验证 ARM32/ARM64 的 APE 解码器、FLAC 解码器和 FLAC 编码器。
- 双 ABI APK 为 19,853,160 字节，比 v1.1.0 小约 14.8%。
- “音悦”封面从约 1.9 MB PNG 压缩为约 33 KB WebP。

### 权限与隐私

- 桌面歌词仅在用户主动允许悬浮窗权限后启动。
- APE 转换、响度分析和歌词处理全部在手机本地完成。
- 应用不申请联网权限。
- 原始 APE 不自动删除；新 FLAC 保存后只替换播放器音乐库条目，避免误删原文件。

### 兼容性与限制

- 最低系统调整为 Android 7.0（API 24）。
- v1.2.0 APK 同时支持 `arm64-v8a` 与 `armeabi-v7a` Android 手机，不包含 x86 模拟器原生库。
- APE 转换会提高 APK 体积，并需要额外的临时存储空间。
- 桌面歌词由用户输入或粘贴；当前版本不访问在线歌词服务。
- 响度统一适合个人本地播放，不替代广播级响度计或母带处理。

## v1.1.0

发布日期：2026-07-30

### 新增

- 新增 FLAC、WAV 文件的导入、播放和响度分析。
- 新增 APE 文件识别、导入、音乐库管理和演唱者分类；播放能力取决于手机系统是否提供 APE 解码器。
- 新增“一键识别并导入手机全部音频”，通过 Android MediaStore 扫描 MP3、FLAC、WAV、APE。
- 新增“导入整个文件夹”，递归识别所选文件夹及子文件夹中的受支持音频。
- 新增“按演唱者分类”，一键切换演唱者分组和原始音乐列表。
- 新增格式标签，音乐列表会显示 MP3、FLAC、WAV 或 APE。
- 新增导入进度状态以及重复文件自动跳过。

### 优化

- 应用封面和桌面图标更换为用户提供的“音悦”图片。
- 文件元数据读取增加容错；即使系统无法解析 APE 标签，也可以用文件名完成导入。
- 旧版音乐库自动兼容，新字段缺失时按 MP3 处理，不清空已有歌曲。
- 全机扫描优先复用 MediaStore 中的标题、演唱者和时长，减少重复读取。
- Android 13 及以上按需申请音频权限；Android 12 及以下使用兼容权限。

### 限制

- APE 不参与响度分析；在没有 APE 系统解码器的手机上可能无法播放。
- 全机扫描只能识别 Android MediaStore 已建立索引的文件；未被系统索引的目录可使用“导入整个文件夹”。

## v1.0.0

发布日期：2026-07-30

### 首次发布

- 本地 MP3 文件导入和播放。
- 播放、暂停、上一首、下一首和进度跳转。
- 基于 ITU-R BS.1770 / EBU R128 思路的整首响度分析。
- 以 `-14 LUFS` 为目标进行非破坏式播放补偿。
- 本地音乐库、分析结果持久化和 Android Material 3 界面。
- GitHub Actions 单元测试、Android Lint 和 Debug APK 自动构建。
